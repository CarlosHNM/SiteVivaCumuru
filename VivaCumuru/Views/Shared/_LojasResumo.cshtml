@model Loja

<div class="col-sm-4 col-md-4 col-lg-4">
    <p><img src="@Model.ImagemUrl" class="img-fluid" asp-append-version="true"></p>
<h4>
    <a asp-controller="Loja" asp-action="Details" asp-route-lojaId="@Model.LojaId">@Model.Nome</a>
    </h4>

    <p>@Model.Descricao</p>
      <div>
        <p class="button">
            <a class="btn btn-primary" id="cartButton" asp-controller="Loja" 
            asp-action="Details" asp-route-LojaId="@Model.LojaId">
            Visualizar dados
        </a>
        </p>
    </div>

</div>
<div>
    <a asp-action="Index" asp-controller="Home" class="btn btn-outline-info" >Início</a>
</div>